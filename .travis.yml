language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t aidanmccarthy/docker-react -f Dockerfile.dev .

script:
  - docker run aidanmccarthy/docker-react npm run test -- --coverage